"1.業務メニューから請求状況を選択する
2.サブメニュー「(医科・DPC)」から起動する
"
"1.「請求状況一覧」ボタンを押下して、「請求状況一覧」画面を表示する。
"
"1.請求状況一覧画面(医科・DPC)の該当の名称ページリンクを押下して、請求状況画面を表示させる
2.「最新状況に更新」ボタンを押下して、最新状況に更新する
"
"１.各レコードの「受付不能件数」ページリンクを
　　押下する
2.受付事務点検ＡＳＰ結果リスト（医科・DPC）を出力する
3.各レコードの「要確認件数」ページリンクを押下する
4.受付事務点検ＡＳＰ結果リスト（医科・DPC）を出力する

"
"１.各レコードの「入院件数」ページリンクを押下する
２.送信データ集計表を出力する
3.各レコードの「入院外件数」ページリンクを押下する
4.送信データ集計表を出力する
"
"１.各レコードの「請求確定件数」ページリンクを押下する
２.オンライン受領書を出力する
"
"１.各レコードの、「送信回」ページリンクを押下する
2.「受付データダウンロード（医科・DPC)」を表示する
3.「戻る」ボタンを押下して、請求状況　（医科）に戻る
"
"1.「CSV一括ダウンロード」ボタンを押下する。
2.「CSV一括ダウンロード」を表示する。
3.「戻る」ボタンを押下して、請求状況画面に戻る
"
"1.「送信抑止解除」ボタンを押下する。
2.「キャンセル」を押下して、処理を中止する。
3.「送信抑止解除」ボタンを押下する。
4.「ＯＫ」を押下して、送信抑止を解除する。

"
1.「戻る」ボタン押下により「請求状況一覧」へ戻る。
"1.業務メニューから請求状況を選択する
2.サブメニュー「(医科・DPC)」から起動する
"
"1.「請求状況一覧」ボタンを押下して、「請求状況一覧」画面を表示する。
"
"1.請求状況一覧画面(医科・DPC)の該当の名称ページリンクを押下して、請求状況画面を表示させる
2.「最新状況に更新」ボタンを押下して、最新状況に更新する"
"１.各レコードの「受付不能件数」ページリンクを
　　押下する
2.受付事務点検ＡＳＰ結果リスト（医科・DPC）を出力する
3.各レコードの「要確認件数」ページリンクを押下する
4.受付事務点検ＡＳＰ結果リスト（医科・DPC）を出力する

"
"１.各レコードの「入院件数」ページリンクを押下する
２.送信データ集計表を出力する
3.各レコードの「入院外件数」ページリンクを押下する
4.送信データ集計表を出力する
"
"１.各レコードの「請求確定件数」ページリンクを押下する
２.オンライン受領書を出力する
"
"１.各レコードの、「送信回」ページリンクを押下する
2.「受付データダウンロード（医科・DPC)」を表示する
3.「戻る」ボタンを押下して、請求状況　（医科）に戻る
"
"1.「CSV一括ダウンロード」ボタンを押下する。
2.「CSV一括ダウンロード」を表示する。
3.「戻る」ボタンを押下して、請求状況画面に戻る
"
"1.「送信抑止」ボタンを押下する。
2.「キャンセル」を押下して、処理を中止する。
3.「送信抑止」ボタンを押下する。
4.「ＯＫ」を押下して、送信抑止をする。

"
"1.「戻る」ボタン押下により「請求状況一覧」へ戻る。
"
"1.業務メニューから請求状況を選択する
2.サブメニュー「(歯科）」から起動する
"
"1.「請求状況一覧」ボタンを押下して、「請求状況一覧」画面を表示する。
"
"1.請求状況一覧画面(歯科)の該当の名称ページリンクを押下して、請求状況画面を表示させる
2.「最新状況に更新」ボタンを押下して、最新状況に更新する"
"１.各レコードの「受付不能件数」ページリンクを
　　押下する
2.受付事務点検ＡＳＰ結果リスト（歯科）を出力する
3.各レコードの「要確認件数」ページリンクを押下する
4.受付事務点検ＡＳＰ結果リスト（歯科）を出力する

"
"１.各レコードの「入院件数」ページリンクを押下する
２.送信データ集計表を出力する
3.各レコードの「入院外件数」ページリンクを押下する
4.送信データ集計表を出力する
"
"１.各レコードの「請求確定件数」ページリンクを押下する
２.オンライン受領書を出力する
"
"１.各レコードの、「送信回」ページリンクを押下する
2.「受付データダウンロード（歯科)」を表示する
3.「戻る」ボタンを押下して、請求状況（歯科）に戻る
"
"1.「CSV一括ダウンロード」ボタンを押下する。
2.「CSV一括ダウンロード」を表示する。
3.「戻る」ボタンを押下して、請求状況（歯科）に戻る"
"1.「送信抑止解除」ボタンを押下する。
2.「キャンセル」を押下して、処理を中止する。
3.「送信抑止解除」ボタンを押下する。
4.「ＯＫ」を押下して、送信抑止解除をする。

"
1.「戻る」ボタン押下により「請求状況一覧」へ戻る。
"1.業務メニューから請求状況を選択する
2.サブメニュー「歯科」から起動する
"
"1.都道府県のプルダウンから、該当の都道府県を選択する
2.「請求状況一覧」ボタンを押下して、「請求状況一覧」画面を表示する。
"
"1.請求状況一覧画面(歯科)の該当の名称ページリンクを押下して
　請求状況画面を表示させる
"
"１.各レコードの「受付不能件数」ページリンクを
　　押下する
2.受付事務点検ＡＳＰ結果リスト（歯科）を出力する
3.各レコードの「要確認件数」ページリンクを押下する
4.受付事務点検ＡＳＰ結果リスト（歯科）を出力する

"
"１.各レコードの「入院件数」ページリンクを押下する
２.送信データ集計表を出力する
3.各レコードの「入院外件数」ページリンクを押下する
4.送信データ集計表を出力する
"
"１.各レコードの「請求確定件数」ページリンクを押下する
２.オンライン受領書を出力する
"
"１.各レコードの、「送信回」ページリンクを押下する
2.「受付データダウンロード（歯科)」を表示する
3.「戻る」ボタンを押下して、請求状況（歯科）に戻る
"
"1.「CSV一括ダウンロード」ボタンを押下する。
2.「CSV一括ダウンロード」を表示する。
3.「戻る」ボタンを押下して、請求状況（歯科）に戻る"
"1.「送信抑止」ボタンを押下する。
2.「キャンセル」を押下して、処理を中止する。
3.「送信抑止」ボタンを押下する。
4.「ＯＫ」を押下して、送信抑止をする。

"
"1.「戻る」ボタン押下により「請求状況一覧」へ戻る。
"
10.13.7.85
